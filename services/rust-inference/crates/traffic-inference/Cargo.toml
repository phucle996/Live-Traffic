# ==============================================================================
# Traffic Inference Benchmark Runner Manifest (crates/traffic-inference/Cargo.toml)
# Bake-Off Benchmark Suite & Parity Validation Runner for Engine A vs Engine B
# ==============================================================================

[package]
name = "traffic-inference"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
rayon = { workspace = true }
traffic-contract = { path = "../traffic-contract" }
traffic-tree-engine = { path = "../traffic-tree-engine" }
traffic-onnx-engine = { path = "../traffic-onnx-engine" }
