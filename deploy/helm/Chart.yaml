# ==============================================================================
# Helm Chart Metadata (deploy/helm/Chart.yaml)
# ==============================================================================

apiVersion: v2
name: traffic-prediction
description: Urban Traffic Prediction System — FastAPI Serving API + Streamlit Dashboard
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - traffic
  - ml
  - prediction
  - fastapi
maintainers:
  - name: Platform Team
    email: platform@traffic-prediction.local
